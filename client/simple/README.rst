=====================
MEMO vite development
=====================

Local install::

  # in folder ./client/simple/
  $ npm install

Fix source code::

  # in folder ./client/simple/
  $ npm run fix

Fix & Build::

  $ ./manage vite.simple.build
